name: Build and Deploy

on:
  push:
    branches:
      - main  # یا هر شاخه‌ای که شاخه اصلی شماست

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages # شاخه‌ای که سایت از روی آن منتشر می‌شود
          folder: . # پوشه‌ای که باید منتشر شود (کل پروژه)